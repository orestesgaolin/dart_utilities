name: git_branches
description: CLI tool to find merged & stale local git branches and batch-delete or push them.
version: 0.2.1
repository: https://github.com/orestesgaolin/dart_utilities
issue_tracker: https://github.com/orestesgaolin/dart_utilities/issues
topics:
  - cli
  - git
  - branches
  - cleanup
  - nocterm

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.7.0
  nocterm: ^0.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  git_branches:
